# Инструменты разработки — в образ на Railway НЕ попадают (см. Dockerfile,
# там ставится только requirements.txt). Версия зафиксирована, чтобы новый
# релиз линтера не «сломал» сборку на ровном месте.
-r requirements.txt
ruff==0.15.22
