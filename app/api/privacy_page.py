"""
Политика обработки персональных данных: GET /privacy.

Текст описывает ТО, ЧТО РЕАЛЬНО ДЕЛАЕТ КОД, и ничего сверх этого — каждое
утверждение здесь можно проверить по исходникам (ссылки на места указаны
в комментариях). Если поведение меняется, правится и эта страница.

Это не юридическое заключение: перед публикацией под конкретное юрлицо
текст стоит показать юристу и дописать реквизиты оператора.
"""
from __future__ import annotations

from app.api.public_style import (
    SITE_CSS, TELEGRAM_CONTACT, head_meta, site_footer, site_nav,
)

# дата последнего пересмотра текста — меняется вместе с содержанием
UPDATED = "21 июля 2026"

_HEAD = """<!doctype html><html lang="ru"><head>""" + head_meta(
    "Обработка персональных данных — Боты для записей",
    "Какие данные собирает бот записи, зачем они нужны, где хранятся "
    "и как их удалить.",
) + """<style>
""" + SITE_CSS + """
.doc{max-width:660px;margin:0 auto}
.doc h2{font:400 22px/1.3 Georgia,serif;margin:48px 0 12px}
.doc h2:first-of-type{margin-top:40px}
.doc p,.doc li{font:400 15.5px/1.65 -apple-system,system-ui,sans-serif;
  color:var(--ink)}
.doc p{margin:0 0 14px}
.doc ul{margin:0 0 14px;padding-left:22px}
.doc li{margin-bottom:8px}
.doc .lead{color:var(--muted);font:400 16px/1.6 Georgia,serif;margin-bottom:8px}
.doc .upd{font:600 12px/1.3 -apple-system,system-ui,sans-serif;
  letter-spacing:.06em;text-transform:uppercase;color:var(--muted);
  margin-bottom:32px}
.doc a{color:var(--gold)}
.panel{background:var(--surface-2);border:1px solid var(--border);
  border-radius:16px;padding:20px 24px;margin:24px 0}
.panel p:last-child{margin-bottom:0}
</style></head><body>
"""


def _section(title: str, *blocks: str) -> str:
    return f"<h2>{title}</h2>" + "".join(blocks)


def render_privacy_page() -> str:
    parts = [_HEAD, site_nav("privacy")]
    parts.append('<main class="wrap"><div class="hero">'
                 '<span class="eyebrow">Документ</span>'
                 '<h1>Обработка персональных данных</h1>'
                 '<p>Коротко и по существу: что бот запоминает, зачем, '
                 'кто это видит и как попросить всё удалить.</p></div>')

    parts.append('<div class="doc">')
    parts.append(f'<p class="upd">Редакция от {UPDATED}</p>')
    parts.append(
        '<p class="lead">Ниже описано фактическое поведение системы. Если '
        'что-то из перечисленного вас не устраивает — не отправляйте форму '
        'и запишитесь по телефону клуба.</p>')

    parts.append(_section(
        "Кто обрабатывает данные",
        "<p>Данные вводятся на странице записи конкретного клуба, салона "
        "или мастера — он решает, зачем они ему нужны, и отвечает за них "
        "перед вами. Мы предоставляем клубу программу и сервер, то есть "
        "обрабатываем данные по его поручению и в тех же пределах.</p>",
        f'<p>Связаться с нами: <a href="{TELEGRAM_CONTACT}">Telegram '
        '@NeoMeal</a>.</p>'))

    parts.append(_section(
        "Что именно собирается",
        "<ul>"
        "<li><b>Имя и телефон</b> — когда вы записываетесь через сайт. "
        "Телефон нужен, чтобы администратор мог связаться с вами, если "
        "занятие переносится или освободилось место.</li>"
        "<li><b>Идентификатор и имя в Telegram или ВКонтакте</b> — когда "
        "вы записываетесь через бота. Пароли и переписка нам недоступны: "
        "мессенджер их не передаёт.</li>"
        "<li><b>Сами записи</b> — на какое занятие, к какому мастеру, "
        "когда записались, отменили или перенесли.</li>"
        "<li><b>Отзывы и оценки</b> — текст, оценка и указанное вами имя. "
        "Отзыв публикуется после проверки; телефон при оценке мастера "
        "нужен только чтобы одна оценка не ставилась дважды, и на сайте "
        "не показывается.</li>"
        "</ul>"
        "<p>Мы не спрашиваем паспортные данные, адрес, дату рождения и "
        "платёжные реквизиты. Если оплата подключена, картой вы платите на "
        "стороне платёжной системы — номер карты к нам не попадает.</p>"))

    parts.append(_section(
        "IP-адрес и файлы cookie",
        "<p>IP-адрес используется только в момент запроса, чтобы ограничить "
        "частоту отправки форм (защита от перебора). Он живёт в оперативной "
        "памяти и в базу не записывается.</p>"
        "<p>Публичные страницы не ставят cookie и не содержат счётчиков, "
        "рекламных пикселей и сторонних скриптов. Одна служебная cookie "
        "появляется только в панели администратора — она хранит вход "
        "владельца бота, а не посетителя.</p>"))

    parts.append(_section(
        "Где данные хранятся и кто их видит",
        "<p>База данных размещена на серверах хостинга Railway. Доступ к "
        "записям своего клуба есть у его администратора — через бота и "
        "панель управления. Данные клубов изолированы: администратор одного "
        "клуба не видит записи другого.</p>"
        '<div class="panel"><p>Раз в сутки создаётся резервная копия базы и '
        "отправляется в закрытый чат в Telegram, доступный только владельцу "
        "платформы. Это защищает от потери данных при сбое хостинга, но "
        "означает, что копия ваших записей хранится и там.</p></div>"))

    parts.append(_section(
        "Кому данные не передаются",
        "<p>Мы не продаём и не передаём данные третьим лицам, не используем "
        "их для рекламы и не рассылаем сообщения помимо тех, что связаны с "
        "вашей записью: подтверждение, напоминание, перенос, отмена.</p>"))

    parts.append(_section(
        "Сколько данные хранятся",
        "<p>Записи и контакты хранятся, пока клуб пользуется сервисом и не "
        "удалит их сам.</p>"
        '<div class="panel"><p>После записи через сайт вы получаете личную '
        "ссылку «Мои записи и данные». На ней можно посмотреть все свои "
        "записи, отменить любую, скачать копию своих данных файлом и удалить "
        "их — сразу, без обращения к кому-либо. Удаление убирает записи, имя, "
        "телефон и оценки, а саму ссылку делает недействительной.</p></div>"
        "<p>Если ссылка потерялась — обратитесь к администратору клуба или "
        "напишите нам. Согласие отзывается тем же способом.</p>"))

    parts.append(_section(
        "Изменения",
        "<p>Если состав собираемых данных изменится, изменится и эта "
        "страница — с новой датой редакции наверху.</p>"))

    parts.append('</div>')
    parts.append(site_footer())
    parts.append('</main></body></html>')
    return "".join(parts)


PRIVACY_HTML = render_privacy_page()
