[pytest]
addopts = -m "not pg"
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    pg: требует настоящей PostgreSQL (запуск: pytest -m pg в CI)
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestUnhandledThreadExceptionWarning
